#
# Copy this file to "deploy/.env" and adjust the variables for your machine.
#

# Path to the Config Server YAMLs (can be relative to the compose file).
# On Windows, prefer an absolute path to avoid volume binding issues.
CONFIG_DIR=../config
