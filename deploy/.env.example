#
# Copie este arquivo para "deploy/.env" e ajuste as variáveis conforme a sua máquina.
#

# Caminho para os YAMLs do Config Server (pode ser relativo ao arquivo compose).
# No Windows, prefira um caminho absoluto para evitar problemas de bind de volume.
CONFIG_DIR=../config
